.root {
    flex-direction: row;
}

.label {
    position: relative;
    top: 2px;
    left: 3px;
    min-width: 90.2px;
}

.arrow {
    top: 2px;
    flex-shrink: 4;
}

.text-parent {
    flex-grow: 1;
    width: 170px;
    flex-direction: row;
    justify-content: space-between;
}

.text-field {
    width: 75px;
    flex-shrink: 1;
}